/**
 * @description
 * This file contains frontend logic related to the search.
 */

document.addEventListener("DOMContentLoaded", () => {
    const search = document.getElementById("search") as HTMLInputElement;
    if (!search) return;
});